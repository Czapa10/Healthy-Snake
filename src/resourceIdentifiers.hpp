#pragma once

namespace Textures
{
    enum ID
    {
        ///splash state
        companyLogo,
        name,
        madeBy,

        ///menu state
        menuBackground1,
        menuBackground2,
        menuBackground3,
        menuBackground4,
        menuBackground5,

        ///game sprites
        gameBackground,
        snakeHead,
        snakeStraightBody,
        snakeTurnBody,
        snakeTail
    };

}

namespace Fonts
{
    enum ID
    {
        pooh
    };
}
