/*
namespace GameElements
{


struct BodyPart
{
    //sf::Vector2f pos;

    int posX;
    int posY;

    enum Direction
    {
        up,
        down,
        right,
        left
    };

    Direction direction;

    enum Sprite
    {
        head,
        straightBody,
        turnBody,
        tail
    };

    Sprite sprite;

    BodyPart(int x, int y, Direction _direction, Sprite _sprite)
    :posX(x)
    ,posY(y)
    ,direction(_direction)
    ,sprite(_sprite)
    {
    }

};
*/

}
