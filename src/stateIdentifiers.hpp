#pragma once

namespace States
{

    enum ID{
        splashState,
        menuState,
        settingsState,
        bestScoresState,
        difficultyLevelChoiceState,
        gameState,
        pauseState
    };

}
