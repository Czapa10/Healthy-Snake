#pragma once

namespace Audio
{
    namespace Sounds
    {
        enum ID
        {
            cat, //to splash screen
            eat,
            buttonClick,
            menuMove
        };
    }
}
