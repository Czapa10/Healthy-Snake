#pragma once

namespace Audio
{
    namespace Sounds
    {
        enum ID
        {
            cat, //to splash screen
            eat,
            die,
            explosion //only for tests
        };
    }
}
